[project]
name = "luna-tutor"
version = "0.1.0"
description = "Grade 3 scripted English teaching core"
requires-python = ">=3.12"
dependencies = [
    "pydantic",
    "httpx",
    "PyYAML",
    "fastapi",
    "uvicorn[standard]",
    "pipecat-ai[openrouter]==1.11.0",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "respx",
    "python-dotenv",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/luna_tutor"]
